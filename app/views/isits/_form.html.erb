<%= form_with(model: isit, local: true) do |form| %>
  <% if isit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(isit.errors.count, "error") %> prohibited this isit from being saved:</h2>

      <ul>
      <% isit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
